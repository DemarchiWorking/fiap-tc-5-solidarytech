# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
override.tf
override.tf.json
*_override.tf
.terraformrc
terraform.rc
terraform.tfvars
*.auto.tfvars
!terraform.tfvars.example

# Config parcial do backend: carrega o nome do bucket de state, que e gerado
# com sufixo aleatorio por conta. Cada integrante do grupo tem o seu.
backend.hcl
!backend.hcl.example

# Credenciais e segredos — NUNCA versionar
# (a Fase 4 versionou secrets.yaml em texto puro; aqui isso e bloqueado por politica)
*secrets.yaml
*secret.yaml
.env
.env.*
!.env.example
credentials
*.pem
*.key
kubeconfig*

# Gerados por ./comecar.sh — contem credenciais e identificacao.
# .env.local guarda a License Key do New Relic e o token do SonarCloud.
.solidarytech.conf
.env.local

# Build / deps
node_modules/
__pycache__/
*.pyc
.venv/
venv/
dist/
build/
coverage.out
coverage.xml
.coverage
*.test

# Evidencias binarias grandes (ficam fora do git; referenciadas no relatorio)
docs/07-evidencias/**/*.mp4
docs/07-evidencias/**/*.mov

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# HTML intermediario do relatorio: gerado por `make relatorio` a partir do
# Markdown, em segundos. O PDF ao lado dele SIM e versionado — e o entregavel
# E3, e quem avalia pode receber so o link do repositorio.
docs/relatorio/RELATORIO-FASE5.html
