apiVersion: v1
kind: Service
metadata:
  name: volunteer-service
  labels:
    app: volunteer-service
spec:
  type: ClusterIP
  selector:
    app: volunteer-service
  ports:
    - name: http
      port: 8083
      targetPort: http
      protocol: TCP
